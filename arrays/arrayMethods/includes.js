//! the includes method checks if the array 'includes' a certain value/object,
//! then returns true or false

const nums(1, 2, 3, 4, 5);
const includesThree = num.includes();
console.log(includesThree)